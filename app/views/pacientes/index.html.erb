<h1>Bem Vindo!</h1>
<h2>Lista de Pacientes</h2>

<ul>
  <% @pacientes.each do |paciente| %>
    <li><%= link_to paciente.nome_completo, paciente %></li>
  <% end %>
</ul>

<%= link_to 'Novo Paciente', new_paciente_path %>
<%= link_to 'Voltar para à Home Page', root_path %>