<%= form_with(model: @consulta, url: consultas_path) do |form| %>
  <div class="field">
    <%= form.label :data %>
    <%= form.datetime_local_field :data, min: Time.current %>
  </div>

  <div class="field">
    <%= form.label :medico_id %>
    <%= form.collection_select :medico_id, Medico.all, :id, :nome, prompt: 'Selecione um médico' %>
  </div>

  <div class="actions">
    <%= form.submit 'Agendar Consulta' %>
  </div>
<% end %>
